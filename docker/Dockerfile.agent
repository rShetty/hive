# Agent Marketplace - Agent Container Image
# This image runs a Hermes-based agent with configurable skills

FROM python:3.11-slim

WORKDIR /app

# Install system dependencies
RUN apt-get update && apt-get install -y \
    curl \
    git \
    && rm -rf /var/lib/apt/lists/*

# Install Python dependencies
RUN pip install --no-cache-dir \
    fastapi==0.109.0 \
    uvicorn[standard]==0.27.0 \
    httpx==0.26.0 \
    python-dotenv==1.0.0

# Copy agent application
COPY agent_app/ /app/

# Harden: run as an unprivileged user instead of root.
RUN useradd --uid 10001 --user-group --create-home --shell /usr/sbin/nologin agent \
    && chown -R agent:agent /app
USER agent

# Expose port
EXPOSE 8000

# Run the agent
CMD ["python", "-m", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
